<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>
    <p>Messages...</p>
    <% locals.messages.forEach(message => { %>
      <p>From <%= message.user %> &lpar;<%= message.added %>&rpar; </p>
      <p><%= message.text %></p>
      <% }) %>

  </body>
</html>

